# Campus Ops Frontend

Simple React + Vite frontend for interacting with the Campus Ops backend.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

By default the frontend assumes the backend API is at `http://localhost:4000/api`. You can override by setting `VITE_API_BASE` in the environment or a `.env` file at the frontend root.
